{
  "name": "qrcode-validation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "browser-sync start --server --files='./*.html, ./*.css'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify js/main.js -o bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "qrcode-reader": "^1.0.4"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "browserify": "^16.5.1"
  }
}